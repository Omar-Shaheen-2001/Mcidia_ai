# دليل النشر - Mcidia Platform Deployment Guide

## 🚀 النشر التلقائي على Replit (موصى به)

عند نشر المنصة على Replit لأول مرة، سيتم تهيئة قاعدة البيانات **تلقائياً** بدون أي تدخل يدوي.

### خطوات النشر:

1. **انقر على زر Publish في Replit**
   - افتح مشروعك في Replit
   - انقر على زر "Publish" أعلى الشاشة
   - اتبع التعليمات لنشر المنصة

2. **انتظر تهيئة قاعدة البيانات**
   - عند أول تشغيل، سيكتشف النظام أن قاعدة البيانات فارغة
   - سيتم تلقائياً:
     - ✅ إنشاء جداول قاعدة البيانات
     - ✅ إضافة الأدوار الأساسية (system_admin, consultant, client, etc.)
     - ✅ إضافة خطط الاشتراك (free, monthly, yearly)
     - ✅ إنشاء حساب الادمن
     - ✅ إضافة الخدمات الاستشارية الافتراضية

3. **تحقق من رسائل النجاح**
   - في سجلات التشغيل (Console Logs)، ابحث عن:
   ```
   ======================================================================
   ✅ PRODUCTION DATABASE INITIALIZATION COMPLETE!
   ======================================================================
   
   📝 ADMIN LOGIN CREDENTIALS:
      🔐 Email: admin@example.com
      🔑 Password: admin123
   ======================================================================
   ```

4. **سجل دخول كادمن**
   - افتح رابط المنصة المنشور
   - انتقل إلى صفحة تسجيل الدخول
   - استخدم البيانات:
     - البريد: `admin@example.com`
     - كلمة المرور: `admin123`

---

## 🔧 التهيئة اليدوية (إذا فشلت التهيئة التلقائية)

إذا لم تعمل التهيئة التلقائية لأي سبب، يمكنك تشغيل السكريبت يدوياً:

### الطريقة 1: عبر Shell في Replit

1. افتح Shell في Replit
2. شغّل الأمر:
   ```bash
   python init_production_db.py
   ```
3. اتبع التعليمات على الشاشة

### الطريقة 2: عبر Run في Replit

1. افتح ملف `init_production_db.py`
2. انقر على زر "Run" في Replit
3. انتظر رسائل النجاح

---

## 📊 ما يتم إنشاؤه تلقائياً

### حساب الادمن:
- **البريد**: admin@example.com
- **كلمة المرور**: admin123
- **الدور**: system_admin (مسؤول النظام)
- **الصلاحيات**: 
  - الوصول الكامل للوحة الادمن
  - عرض وإدارة جميع المستخدمين
  - عرض وتصدير مشاريع جميع المستخدمين
  - إدارة الخدمات والعروض
  - إدارة خطط الاشتراك

### الأدوار الأساسية:
1. `system_admin` - مسؤول النظام
2. `consultant` - مستشار محترف
3. `company_user` - مستخدم شركة
4. `client` - عميل فردي
5. `external_user` - مستخدم خارجي (عضو منظمة)

### خطط الاشتراك:
1. **Free** - مجاني (1,000 رصيد AI شهرياً)
2. **Monthly** - شهري ($99/شهر، 100,000 رصيد AI)
3. **Yearly** - سنوي ($999/سنة، 1,500,000 رصيد AI)
4. **Pay per use** - الدفع حسب الاستخدام

### الخدمات الاستشارية:
1. **البناء المؤسسي والخدمات الإدارية**
   - التخطيط الاستراتيجي وبناء مؤشرات الأداء
   - بناء الهوية الاستراتيجية
   - بناء إطار الحوكمة

2. **خدمات الموارد البشرية**
   - تصميم الهيكل التنظيمي
   - بناء الوصف الوظيفي

3. **الخدمات المالية والمحاسبية**
   - التحليل المالي
   - التخطيط المالي والميزانية

4. **خدمات التسويق**
   - استراتيجية التسويق

---

## 🔍 استكشاف الأخطاء

### المشكلة: لا تظهر رسالة النجاح

**الحل:**
1. تحقق من سجلات التشغيل (Console Logs)
2. ابحث عن أخطاء في السجلات
3. إذا وجدت خطأ "Auto-initialization failed"، شغّل السكريبت يدوياً:
   ```bash
   python init_production_db.py
   ```

### المشكلة: لا يمكن تسجيل الدخول كادمن

**الحل:**
1. تأكد من استخدام البريد الصحيح: `admin@example.com`
2. تأكد من استخدام كلمة المرور الصحيحة: `admin123`
3. إذا لم يعمل، أعد تشغيل السكريبت اليدوي:
   ```bash
   python init_production_db.py
   ```
4. عند السؤال "Continue? (yes/no):"، اكتب `yes`

### المشكلة: لا تظهر الخدمات الاستشارية

**الحل:**
1. سجل دخول كادمن
2. افتح Shell وشغّل:
   ```bash
   python init_production_db.py
   ```
3. اكتب `yes` عند السؤال
4. تحقق من ظهور رسالة "✅ Created X services"

---

## 🔐 أمان قاعدة البيانات

### مهم جداً:
- ⚠️ **غيّر كلمة مرور الادمن فوراً بعد أول تسجيل دخول**
- قاعدة البيانات الإنتاجية منفصلة تماماً عن بيئة التطوير
- لا يتم مشاركة أي بيانات بين البيئتين
- كل نشر يبدأ بقاعدة بيانات فارغة ويتم تهيئتها تلقائياً

### تغيير كلمة مرور الادمن:
1. سجل دخول بحساب الادمن
2. انتقل إلى صفحة الملف الشخصي
3. غيّر كلمة المرور إلى كلمة مرور قوية

---

## 📝 ملاحظات إضافية

### متطلبات البيئة الإنتاجية:
- ✅ PostgreSQL database (يتم توفيره تلقائياً من Replit)
- ✅ Python packages (تم تثبيتها مسبقاً)
- ✅ Environment variables (يتم إعدادها تلقائياً)

### الملفات المهمة:
- `app.py` - نقطة الدخول الرئيسية، تحتوي على منطق التهيئة التلقائية
- `init_production_db.py` - سكريبت التهيئة اليدوية
- `models.py` - نماذج قاعدة البيانات

### عند إضافة خدمات جديدة:
يمكنك إضافة خدمات جديدة من لوحة الادمن:
1. سجل دخول كادمن
2. انتقل إلى `/admin/services`
3. انقر على "Add New Service"
4. املأ البيانات واحفظ

---

## ✅ قائمة التحقق للنشر

- [ ] تم نشر المنصة على Replit
- [ ] ظهرت رسالة "DATABASE INITIALIZATION COMPLETE" في السجلات
- [ ] تم تسجيل الدخول بنجاح كادمن (admin@example.com / admin123)
- [ ] تظهر الخدمات الاستشارية في صفحة `/services`
- [ ] يمكن الوصول للوحة الادمن `/admin`
- [ ] تم تغيير كلمة مرور الادمن لكلمة مرور قوية
- [ ] تم إنشاء حسابات المستخدمين الأساسية (إن وجدت)

---

## 🆘 الدعم

إذا واجهت أي مشكلة:
1. تحقق من سجلات التشغيل (Console Logs)
2. شغّل السكريبت اليدوي `python init_production_db.py`
3. راجع قسم "استكشاف الأخطاء" أعلاه
4. تأكد من توفر متغيرات البيئة الصحيحة (DATABASE_URL)
